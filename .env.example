# Copy thành .env (đã gitignore — KHÔNG BAO GIỜ commit key; repo là PUBLIC)
OPENROUTER_API_KEY=sk-or-v1-...
DEEPSEEK_API_KEY=sk-...
POSTGRES_PASSWORD=lawstate
# Local override (mặc định đã hợp lý):
# DATABASE_URL=postgresql://lawstate:lawstate@localhost:5432/lawstate
# HF_CACHE=~/.cache/huggingface          # mount vào container api
# TEST_DATABASE_URL=...                  # bật heavy DB tests (fixture DROP SCHEMA — chỉ trỏ DB vứt được)
